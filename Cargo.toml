[package]
name = "habit_tracker"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
sha2 = "0.10"        # password hashing
hex = "0.4"          # encode hash digest as a string
chrono = { version = "0.4", features = ["serde"] }
